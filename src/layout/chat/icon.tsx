
const CIconChatbox = ({ onHandleCollapseDown }: any) => {

  return (
    <div className='chatbox__icon' onClick={() => onHandleCollapseDown()}>
      chat
    </div>
  )
}

export default CIconChatbox
